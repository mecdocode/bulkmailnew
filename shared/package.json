{
  "name": "@bulk-email/shared",
  "version": "1.0.0",
  "description": "Shared types and contracts for bulk email sender",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  },
  "files": [
    "dist"
  ]
}
