{
  "buildCommand": "pnpm --filter shared build && pnpm --filter client build && node scripts/copy-dist.js",
  "outputDirectory": "dist",
  "functions": {
    "api/*.js": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
