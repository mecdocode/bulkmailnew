{
  "name": "bulk-email-sender",
  "version": "1.0.0",
  "description": "Lightweight bulk email sending tool with React frontend and Express backend",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter client dev\" \"pnpm --filter server dev\"",
    "build": "pnpm --filter client build",
    "vercel-build": "pnpm --filter shared build && pnpm --filter server build && pnpm --filter client build",
    "start": "pnpm --filter server start",
    "clean": "pnpm --filter client clean && pnpm --filter server clean",
    "type-check": "pnpm --filter client type-check && pnpm --filter server type-check"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
